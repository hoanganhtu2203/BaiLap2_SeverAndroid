<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</head>

<body>
    <div class="container" style="width: 500px; margin-top: 50px;">
        <form class="row g-3">
            <P class="container text-center" style="font-weight: bold; font-size: 24px;color: blue;">TÍNH TOÁN</P>

            <div class="col-md-12">
                <label for="num1" class="form-label">Số thứ nhất :</label>
                <input type="Số thứ nhất" class="form-control" name="num1" id="num1" placeholder="num1" step="0.1" required>
            </div>
            <div class="col-md-12">
                <label for="num2" class="form-label">Số thứ hai : </label>
                <input type="Số thứ hai" class="form-control" name="num2" id="num2" placeholder="num2" step="0.1" required>
            </div>

            <div class="col-md-3">
                <label for="operator" class="form-label">Phép tính : </label>
                <select id="operator" name="operator" class="form-select">
                    <option selected>Choose...</option>
                    <option value="tong">+</option>
                    <option value="hieu">-</option>
                    <option value="tich">*</option>
                    <option value="thuong">/</option>
                </select>
            </div>
            <div class="col-md-12">
                <button type="submit" class="btn btn-primary" value="Kết quả">Tính</button>
                <h3>Kết quả : </h3>
                {{#if showResult}}
                <h3>{{a}} {{opera}} {{b}} = {{kq}}</h3>
                {{/if}}
            </div>
        </form>
    </div>
</body>

</html>